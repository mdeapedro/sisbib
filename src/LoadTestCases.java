public class LoadTestCases {
    public static void loadTestCases(String users, String books, String copies) {
        loadUsers(users);
        loadBooks(users);
        loadCopies(users);
    }

    private static void loadUsers(String users) { }
    private static void loadBooks(String books) { }
    private static void loadCopies(String copies) { }
}
